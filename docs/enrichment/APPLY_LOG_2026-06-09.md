# Enrichment Apply Log — 2026-06-09

Resumed the dynamic enrichment workflow (original run `wf_0021d0c6-5ce`, cut off by session limit; plans salvaged to this directory). Executed as two orchestrated workflow runs (`wf_48f62281-bf0`, `wf_66c9689a-186`; 34 agents total).

## What was done

### 1. Apply pass (cat100 + cat531, confirmed opportunities)
Scope: all verified-`confirmed` accuracy_fix/currency_update items plus high-priority confirmed items of other types, from `_recovered_sections.json` (sliced per chapter in `_slices/apply_*.json`).

- **Applied: 237 / 246** (cat100 110, cat531 127)
- Skipped, already applied in the pre-crash session: 6
- Skipped, gap analysis did not match actual file content: 3
  - cat531/02 block targeting 08/08 ("Readiness Review" identity mismatch — its re-derived fixes were applied to the correct file `cat531-supplement/08/08.*`)
  - cat531/06 BigAL→TeachGen@i renames (premise false: BigAL is used platform-wide)
- Errors: 0

### 2. Verification pass (the stage the original run never reached)
Scope: unverified high-priority accuracy_fix/currency_update items — ail606 (all 64 sections were gap-stage), cat531 gap-stage chapters, cat100 leftovers. Slices in `_slices/verify_*.json`; per-item verdicts in `_slices/verified_*.json`.

- **Confirmed and applied: 44** (ail606 27, cat531/cat100 17)
- **Refuted, not applied: 3**
  - cat531/06/04: BigAL→TeachGen@i rename — two-name usage is intentional (BigAL = platform tutor, TeachGen@i = CAT531 scenario assistant)
  - ail606/04/06 + 04/07: "Codex is a deprecated brand" — false; OpenAI Codex is a live flagship coding agent as of 2026
- Unverifiable: 0

Representative verified fixes: WCAG contrast example 1.7:1→1.9:1 (recomputed via luminance formula); weighted-rubric 92%/88% pair was mathematically impossible→92%/81% with pinned weights; COPPA amended-rule dates confirmed against the Federal Register (full compliance 2026-04-22); `git branch -M main` inserted before first push (Git 2.28 default-branch behavior); Mayer dual-channel contradiction between mdx and misconceptions.json reconciled to the canonical sensory-modality reading; rail_action vocabulary reconciled against the backend enum in `researchService.js` (one proposed "fix" rejected because it would have written out-of-vocabulary values).

### 3. Quality gates — all green (run twice, after each pass)
content-validator (256 sections, 0 errors), mojibake grep 0, self-closing interactive tags 0, LLM artifacts 0, all modified JSON parses, eslint 0, vitest 127/127, vite build OK. One content-validator failure after pass 1 was auto-fixed (fix round 1).

## Deferred (intentionally out of scope)
- Confirmed non-high-priority non-accuracy types (evidence_add/new_callout/scaffolding etc.): cat100 69, cat531 37
- Unverified medium/low items: cat100 246, cat531 316, ail606 436 (incl. assessment_align/accessibility types at high priority)
- Course-wide BigAL/TeachGen@i naming sweep (needs a deliberate decision, see refuted items)
- ail606 still carries generic storyboard artifact labels in several ch04 meta files (out-of-slice observation)

## Deploy note
`frontend/public/api/book/` static snapshots were hand-updated **only for ail606/01** by one agent; all other changed sections' snapshots are stale. Run `node scripts/export_static_content.mjs` against a local backend, then rebuild and `wrangler pages deploy` before the live site reflects this content.
