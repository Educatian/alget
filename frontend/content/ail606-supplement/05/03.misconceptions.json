{
  "section_id": "ail606_supplement_5_3_task_scenarios_for_prototype_testing",
  "misconceptions": [
    {
      "id": "ail606_supplement_5_3_leading_task_steps",
      "pattern": "task_embeds_solution_path",
      "description": "Writing a task that names the exact controls and order, so it tests instruction-following rather than findability",
      "trigger": "learner's task lists the buttons or steps the participant should click",
      "feedback": "Strip the click path. State the goal and context and let the participant find the route, so the design is what gets tested.",
      "rail_action": "represent"
    },
    {
      "id": "ail606_supplement_5_3_goal_vs_instruction",
      "pattern": "instruction_instead_of_goal",
      "description": "Framing the task as an instruction to perform rather than a realistic goal to accomplish",
      "trigger": "learner phrases the task as 'do X then Y' instead of a situation with a desired end-state",
      "feedback": "Rewrite as a goal-based scenario: give the participant a realistic motivation and end-state, not a procedure.",
      "rail_action": "explain"
    },
    {
      "id": "ail606_supplement_5_3_no_success_condition",
      "pattern": "missing_success_condition",
      "description": "Writing an open-ended or vague task with no observable success criterion the moderator can score",
      "trigger": "learner's task is 'explore' or 'tell us what you think' with no defined completion state",
      "feedback": "Define what counts as completing the task so the session yields evidence about a specific workflow, not just impressions.",
      "rail_action": "ask"
    },
    {
      "id": "ail606_supplement_5_3_vocabulary_leak",
      "pattern": "task_reuses_interface_vocabulary",
      "description": "Copying an interface label into the task wording (the interface says 'Template Library' and the task says 'open the template library'), which hands the participant the exact search term findability was meant to test.",
      "trigger": "task contains an interface noun the participant could not have known on their own",
      "feedback": "Use the user's words, not the interface's. If the task names the label or the route, the participant only follows directions and the findability question is answered for them in the prompt. State the destination, never the route, and rewrite any task that leaks an interface noun.",
      "rail_action": "represent"
    }
  ]
}
